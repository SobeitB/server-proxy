{
    "version": 2,
    "devCommand": "npm run start",
    "builds": [
        {
            "src": "./index.js",
            "use": "@vercel/node"
        }
    ],

    "rewrites": [
        {
            "source": "/allPools/:network/:page",
            "destination": "/allPools"
        }
    ]
}